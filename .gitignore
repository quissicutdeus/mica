node_modules
.idea
dist
playwright-report

# Generated by `vite build -c vite.addon.config.ts` — the add-on bundles, gitignored like
# any other build output.
web/public/addons/

# Destructive dev-only schema reset, regenerate with: pnpm generate:sql --with-reset
sql/dev-reset.sql

# Scratch mockups from Claude's visual brainstorming companion
.superpowers/
.agent/
.env*
# Assistant config is shared, like AGENTS.md — see §2.10. Personal overrides are not,
# and neither is anything Claude Code writes per-machine: `worktrees/` is scratch it
# creates and removes on its own. The `.worktrees/` line below does not cover it —
# a pattern with no leading slash still has to match the whole path segment.
.claude/settings.local.json
.claude/worktrees/
.worktrees/
