# client agent memory

One file per finding; this index is what loads. Read the file before relying on
its one-line summary. Nothing is recorded yet: the first native that wanted an
argument order its types did not say, or the first framework whose client object
differed from its docs, goes in its own file and gets a line here.
