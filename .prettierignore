node_modules
dist
.svelte-kit
build
pnpm-lock.yaml

# Playwright output. Gitignored, but Prettier does not read .gitignore, so a
# failed run leaves an error-context.md that turns `pnpm format:check` red.
playwright-report
test-results
blob-report

# TypeDoc output. Same reason as above.
web/docs

# Subagent-driven-development scratch state (ledgers, task briefs, review
# packages). Gitignored via .superpowers/sdd/.gitignore, same reason as above.
.superpowers

# The remember plugin's session scratch. Gitignored via .remember/.gitignore,
# same reason as above.
.remember

# pnpm's content-addressed store, when a runner keeps it inside the workspace
# rather than under $HOME -- forgejo-runner's job containers do. Prettier does
# not read .gitignore, and a store file is JavaScript with no extension or a
# shebang that is not a stylesheet, so a walk into it is a red format gate.
.pnpm-store
