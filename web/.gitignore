# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test artifacts
playwright-report
test-results
# Each `--shard` writes one of these; the merge job turns the set back into a
# single playwright-report. Only CI produces them, but a local shard run would too.
blob-report

# Generated docs
docs

